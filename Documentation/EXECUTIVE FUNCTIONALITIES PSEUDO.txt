//SEE FELLOW EXECUTIVES, MANAGERS AND EMPLOYEES

START

1.Set UserTypeID and UserName = UserTypeID and UserName in DB.
2.If{UserTypeID == Managers && Employees && Executives}
	THEN
	Display Usernames of recipients in ChatboxUI
  End If

END


//HAVE CHOICE TO BE INVISIBLE TO EMPLOYEES AND MANAGERS BUT NOT OTHER EXECUTIVES

START

1.Set UserTypeID and UserName = UserTypeID and UserName in DB.
2.Select Checkbox to be invisible
3.If{recipient User_ID != executive UserType_ID}
	Hide Username in recipient chatbox
  Else
	Display Username in recipient chatbox
  End If

End


//BE ABLE TO CREATE CHATROOMS
START

1.Set UserTypeID and UserName = UserTypeID and UserName in DB.
2.Select Button to create chatroom
3.Display("Create Chatroom", "Select Employee to create Chatroom")
	If(Create Chatroom is selected)
		THEN
		Display("Send Invite to the following Users?")
		If {Yes}
			THEN
			Display list of Users
			Select Users to send invitation
			Display(Send, Cancel)
			If{Send}
				Send Invitation to Users
				Start Chatroom 
			Else
				Cancel Action
			End If	
		Else 
			Start Chatroom
 		End If
	
	ELSEIF(Select Employee to create Chatroom is selected)
			THEN
			Display(List of UserNames with Employee_UserTypeID)
			Print("Select the Employee you wish to grant permission to create chatroom")
			Select Employee
			Display("Send Link to Employee to create chatroom?)
			If {Yes}
				THEN
				Send Link
			Else
				Go back to employee selection page
			End If
	END IF
		
END


//BE ABLE TO ACCESS THE DASHBOARD

START

1.Set UserId and UserName = UserId and UserName in DB.
2.Select Button to access dashboard
3.Display("Go To Dashboard?")
4.If(UserTypeID != Employee UserTypeID)
	THEN
	Open Dashboard
  Else
	Print("You are not authorized access to the dashboard")
  EndIF

END


