ADMIN FUNCTIONALITIES PSEUDO_CODE

//LOGIN

START

1.SET Admin_Username && Admin_Password && Admin_Number == null
2.INPUT Username && Password && Number
3.IF {Username == Admin_Username && Password = Admin_Password && Number = Admin_Number}
	Then Log In
	Display Dashboard
  ELSE
	PRINT ("Admin Username or Password may be incorrect")
	CLEAR Username && Password && Number
  ENDIF
END


//LOGOUT

START

1.PRINT ("Are you sure you wish to logout")
2.IF {Yes}
	End User Session
  ELSE
	Display Dashboard
  ENDIF

END


//ADD NEW USER

START

1.DECLARE UserName as string
	 UserID as Integer
	 Number as Integer
	 Password as string
2.SET Username && UserID && Number && Password == null
3.DISPLAY "Enter New User Details in the fields below"
4.GET Username && UserID && Number && Password
8.IF{Username && Number exist in DB}
	PRINT ("User already Exists!")
	CANCEL Action	
  ELSE
	STORE Values in System
	CREATE new User
  ENDIF
9.DISPLAY Dashboard

END


//MODIFY USER

START

1.SELECT Username
2.DISPLAY (Modify/ Delete)
3.SELECT Modify
4.APPLY necessary changes to Account
5.SELECT Save
6.DISPLAY Dashboard

END


//DELETE USER

START

1.SELECT Username
2.DISPLAY (Modify/ Delete)
3.SELECT Delete
4.PRINT ("Do you want to delete this account")
5.IF {Yes}
	THEN Delete Account
  ELSE
	CANCEL Action
  ENDIF
6.DISPLAY Dashboard

END


//VIEW USER STATUS

START

1.SELECT Username
2.IF {User = Online} 
	THEN Print "Online"
  ELSE 
	PRINT "Offline"
  ENDIF

END


//VIEW USER LOG

START

1.SELECT Username
2.FETCH User Activity Log from System
3.DISPLAY User Activity Log

END


//ADD / REMOVE PRIVILEGES

START

1.SELECT Username
2.DISPLAY Privileges
3.TICK / UNTICK checkboxes
4.IF {checkbox is filled}
	THEN enable privilege
  ELSE
	DISABLE privilege
  ENDIF  
END


//SET RESTRICTIONS

START

1.SELECT Username
2.DISPLAY Account restrictions
3.TICK / UNTICK Checkbox
4.IF {Check box is filled}
	THEN Disable Account
	PRINT("This account has been disable successfully")
  ELSE
	ENABLE Account
	PRINT("This account has been enabled successfully")
  ENDIF
  END

