SEE FELLOW MANAGERS AND EMPLOYEES

START

1.Set UserId and UserName = UserId and UserName in DB.
2.If recipients User_Id != executive User_Id
	Then Display recipients UserName If User still exits in DB
End If
END


HAVE CHOICE TO BE INVISIBLE TO EMPLOYEES BUT NOT OTHER MANAGERS AND EXECUTIVES

START

1.Set UserId and UserName = UserId and UserName in DB.
2.Select Checkbox to be invisible
3.If{Recipient UserID == employee UserId}
	Hide UserName in recipient chatbox
  Else
	Display UserName in recipient chatbox
  End If

  End


BE ABLE TO CREATE CHATROOMS

START

1.Set UserId and UserName = UserId and UserName in DB.
2.Select Button to create chatroom
3.Print("Send Invite to the following Users?")
4.If {Yes}
	Then Display list of Users
	Select Users to send invitation
	Display(Send, Cancel)
		If{Send}
			Send Invitation to Users
			Start Chatroom 
		Else
			Cancel Action
		End If
   Else 
	Start Chatroom
   End If

END
